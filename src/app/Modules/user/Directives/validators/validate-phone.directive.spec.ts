import { ValidatePhoneDirective } from './validate-phone.directive';

describe('ValidatePhoneDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatePhoneDirective();
    expect(directive).toBeTruthy();
  });
});
